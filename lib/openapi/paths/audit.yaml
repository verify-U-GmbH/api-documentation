post:
    tags:
      - audit
    summary: audit/{id}
    description: Creates an audit call for a client.
    operationId: createAuditById
    parameters:
      - in: path
        name: body
        description: Address data
        required: true
        schema:
          $ref: ../definitions/create_audit_request.yaml
    responses:
      "200":
        description: successful operation
        content:
          application/json:
            schema:
              $ref: ../definitions/create_audit_response.yaml
      "422":
        description: Client side error
        content:
          application/json:
            schema:
              $ref: ../definitions/client_error.yaml
      "500":
        description: Error