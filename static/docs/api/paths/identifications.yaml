get:
  tags:
    - identifications
  summary: identifications
  description: Returns the last 10 identifications as a list
  operationId: getIdentificationsList
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: ../definitions/identifications_list.yaml
