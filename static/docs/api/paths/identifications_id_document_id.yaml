get:
  tags:
    - identifications
  summary: identifications/id-document/{id}
  description: Returns id-document file uploaded during identification
  operationId: getIdentificationDocumentById
  parameters:
    - in: path
      name: id
      required: true
      description: ID of the id-document
      schema:
        type: string
        format: uuid
  responses:
    "200":
      description: ID document image in JPG format
      content:
        image/jpeg:
          schema:
            type: string
            format: binary
    "422":
      description: Client side error
    "500":
      description: Error
